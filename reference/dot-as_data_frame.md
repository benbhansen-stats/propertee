# Convert object to `data.frame` or produce meaningful error

Convert object to `data.frame` or produce meaningful error

## Usage

``` r
.as_data_frame(x)
```

## Arguments

- x:

  An object

## Value

`x` as a `data.frame`
